-r requirements.txt
# faster-whisper (CTranslate2 backend) — no torch dependency, much lighter
# than SenseVoice/FunASR. Pulls in onnxruntime (used both for faster-whisper's
# own vad_filter=True and our standalone stt/silero_vad.py gate).
faster-whisper>=1.0,<2.0
onnxruntime>=1.18,<2.0
